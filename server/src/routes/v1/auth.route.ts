import { Router } from "express";

const router = Router();

router.post("/login", );
router.post("/register");
router.post("/reset");
router.post("/resetpassword");
router.post("/sendOTP");

export default router;